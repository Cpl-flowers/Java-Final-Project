package Assests;

class goblin extends entity {
    // attributes health, attack, defense, name
    health = 20;
    attack = 5;
    defense = 1;

    // methods
    @Override
    public static void attack() {
        monsterdamage = new java.util.Random().nextInt(attack);
        playerHP -= attack;
    }
}